import React from "react";
import { Container } from "react-bootstrap";

const Contact = () => {
    return (
        <Container
            id="contact"
            fluid
            className="wrapper section--wrapper__contact"
        ></Container>
    );
};

export default Contact;
