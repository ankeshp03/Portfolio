import React from "react";

// const generateLines = (lineCount) => {
//     let lines = [];
//     for (let line = 0; line < lineCount; line++) {
//         const lineSegmentCount = Math.floor(Math.random()) * 3 + 3;
//         const lineSegments = [];
//         let totalWidth = 0;

//         for (let segment = 0; segment < lineSegmentCount; segment++) {
//             lineSegments.push(<span></span>);
//         }
//         lines.push();
//     }
// };

const CodeView = () => {
    // const lineCount = 25;
    return (
        <div className="container__code-view">
            <div></div>
        </div>
    );
};

export default CodeView;
