import React from "react";
import { Container } from "react-bootstrap";

const About = () => {
    return (
        <Container
            id="about"
            fluid
            className="wrapper section--wrapper__about"
        >
            <h2 className="section--header">About</h2>
        </Container>
    );
};

export default About;
